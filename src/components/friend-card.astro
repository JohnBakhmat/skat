---
interface Props {
	name: string;
	logo?: string;
}

const { name, logo } = Astro.props;
---

<div
	class="flex flex-col border-2 border-white rounded-3xl p-5 md:p-10 col-span-2"
>
	<p class="text-2xl md:text-4xl">{name}</p>
	<img src={logo} alt={name} class="max-h-24 w-fit" />
</div>
